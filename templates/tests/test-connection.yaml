apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sonobuoy.fullname" . }}-test-connection"
  labels:
    {{- include "sonobuoy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sonobuoy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
